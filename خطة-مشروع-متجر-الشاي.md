# خطة مشروع متجر الشاي الإلكتروني

**التقنيات:** Next.js · Tailwind CSS · PostgreSQL + Prisma · Stripe + بوابة دفع خليجية

---

## 1. تدفق الدفع المقترح

1. العميل يختار المنتجات → السلة
2. عند الدفع: نحدد **الدولة** → إذا سعودية/عمان/خليج نعرض بوابة الدفع المحلية، وإلا Stripe
3. Webhook من البوابة يحدّث حالة الطلب في قاعدة البيانات تلقائياً (لا تعتمد فقط على redirect الناجح من الواجهة)
4. إرسال إيميل/واتساب تأكيد الطلب

## 2. مميزات لا تنساها لسوق الخليج

- دعم ثنائي اللغة (عربي/إنجليزي) مع RTL كامل في Tailwind
- العملات المتعددة: SAR, OMR, AED... أو تسعير موحد بالدولار مع تحويل عرضي
- حاسبة الشحن حسب الدولة (خصوصاً عمان لو الشحن من السعودية)
- خانة الرقم الضريبي/VAT إذا فيه عملاء تجاريين
- التوافق مع Apple Pay ضروري جداً في السعودية والخليج

---

## 3. هيكل المشروع الكامل

```
tea-shop/
├── prisma/
│   ├── schema.prisma
│   ├── seed.ts                          # بيانات تجريبية (منتجات، فئات)
│   └── migrations/
│
├── public/
│   ├── images/
│   │   └── products/
│   ├── logo.svg
│   └── favicon.ico
│
├── src/
│   ├── app/
│   │   ├── layout.tsx                   # Layout رئيسي (يحدد dir="rtl"/"ltr")
│   │   ├── globals.css                  # Tailwind base + متغيرات الخط العربي
│   │   ├── page.tsx                     # الصفحة الرئيسية
│   │   ├── not-found.tsx
│   │
│   │   ├── (shop)/
│   │   │   ├── layout.tsx               # Header + Footer + CartDrawer
│   │   │   ├── products/
│   │   │   │   ├── page.tsx             # كل المنتجات + فلاتر
│   │   │   │   └── [slug]/page.tsx      # صفحة منتج واحد
│   │   │   ├── category/
│   │   │   │   └── [slug]/page.tsx
│   │   │   ├── cart/
│   │   │   │   └── page.tsx
│   │   │   ├── checkout/
│   │   │   │   ├── page.tsx             # يحدد الدولة ← يختار البوابة
│   │   │   │   ├── success/page.tsx
│   │   │   │   └── cancel/page.tsx
│   │   │   ├── account/
│   │   │   │   ├── page.tsx             # الملف الشخصي
│   │   │   │   ├── orders/page.tsx      # تتبع الطلبات
│   │   │   │   └── addresses/page.tsx
│   │   │   └── about/, contact/, faq/
│   │
│   │   ├── (auth)/
│   │   │   ├── login/page.tsx
│   │   │   ├── register/page.tsx
│   │   │   └── forgot-password/page.tsx
│   │
│   │   ├── admin/
│   │   │   ├── layout.tsx               # محمي بصلاحية ADMIN فقط
│   │   │   ├── page.tsx                 # لوحة تحكم (إحصائيات مبيعات)
│   │   │   ├── products/
│   │   │   │   ├── page.tsx
│   │   │   │   ├── new/page.tsx
│   │   │   │   └── [id]/edit/page.tsx
│   │   │   ├── orders/
│   │   │   │   ├── page.tsx
│   │   │   │   └── [id]/page.tsx        # تحديث حالة الطلب + الشحن
│   │   │   └── categories/page.tsx
│   │
│   │   └── api/
│   │       ├── auth/[...nextauth]/route.ts
│   │       ├── products/route.ts
│   │       ├── orders/
│   │       │   ├── route.ts             # إنشاء طلب جديد
│   │       │   └── [id]/route.ts
│   │       ├── checkout/
│   │       │   ├── stripe/route.ts      # إنشاء جلسة دفع Stripe
│   │       │   └── local-gateway/route.ts  # إنشاء جلسة دفع محلية (Tap/Moyasar)
│   │       ├── webhooks/
│   │       │   ├── stripe/route.ts      # تأكيد الدفع + تحديث الطلب
│   │       │   └── local-gateway/route.ts
│   │       ├── shipping/
│   │       │   └── calculate/route.ts   # حاسبة الشحن حسب الدولة
│   │       └── notifications/
│   │           ├── email/route.ts       # إرسال إيميل تأكيد
│   │           └── whatsapp/route.ts    # إرسال واتساب (عبر WhatsApp Business API)
│   │
│   ├── components/
│   │   ├── ui/                          # أزرار، إنبوت، مودال... (shadcn-style)
│   │   ├── layout/
│   │   │   ├── Header.tsx
│   │   │   ├── Footer.tsx
│   │   │   └── LanguageSwitcher.tsx     # تبديل عربي/إنجليزي (RTL/LTR)
│   │   ├── shop/
│   │   │   ├── ProductCard.tsx
│   │   │   ├── ProductGrid.tsx
│   │   │   ├── CartDrawer.tsx
│   │   │   ├── CurrencySelector.tsx     # SAR / OMR / AED / USD
│   │   │   └── CountrySelector.tsx      # يحدد بوابة الدفع + الشحن
│   │   ├── checkout/
│   │   │   ├── PaymentMethodPicker.tsx  # يعرض Stripe أو البوابة المحلية حسب الدولة
│   │   │   ├── ShippingCalculator.tsx
│   │   │   └── VatField.tsx             # حقل الرقم الضريبي (اختياري)
│   │   └── admin/
│   │       ├── OrdersTable.tsx
│   │       └── ProductForm.tsx
│   │
│   ├── lib/
│   │   ├── prisma.ts                    # Prisma client singleton
│   │   ├── stripe.ts                    # إعداد Stripe SDK
│   │   ├── payment-gateway.ts           # إعداد Tap/Moyasar SDK
│   │   ├── auth.ts                      # إعداد NextAuth
│   │   ├── shipping-rates.ts            # جدول أسعار الشحن حسب الدولة
│   │   ├── currency.ts                  # تحويل ومنطق العملات
│   │   └── i18n/
│   │       ├── ar.json
│   │       └── en.json
│   │
│   ├── hooks/
│   │   ├── useCart.ts
│   │   └── useCountry.ts
│   │
│   ├── store/
│   │   └── cart-store.ts                # Zustand أو Context للسلة
│   │
│   └── types/
│       └── index.ts
│
├── middleware.ts                        # حماية /admin + تحديد اللغة تلقائياً
├── next.config.js
├── tailwind.config.ts                   # إعداد RTL + خطوط عربية
├── .env.example
│   # DATABASE_URL=
│   # STRIPE_SECRET_KEY=
│   # STRIPE_WEBHOOK_SECRET=
│   # LOCAL_GATEWAY_API_KEY=            (Tap أو Moyasar)
│   # LOCAL_GATEWAY_WEBHOOK_SECRET=
│   # NEXTAUTH_SECRET=
│   # WHATSAPP_API_TOKEN=
└── package.json
```

---

## 4. ملاحظات تنفيذية سريعة على الهيكل

- **`(shop)` و `(auth)`**: مجلدات مجمّعة (Route Groups) بدون تأثير على الرابط، تفيد بفصل الـ layouts.
- **`middleware.ts`**: نقطة واحدة لحماية `/admin` والتحقق من الجلسة، وأيضاً مكان جيد لتحديد اللغة/الدولة من الهيدر أو الكوكيز.
- **`CountrySelector.tsx`**: هذا العنصر هو قلب منطق "أي بوابة دفع تظهر" — يحفظ اختيار الدولة في الكوكيز أو localStorage ليُستخدم في الشحن والعملة والدفع معاً.
- **Webhooks منفصلة** لكل بوابة لأن شكل الـ payload يختلف بين Stripe والبوابة المحلية.
- **`shipping-rates.ts`**: جدول بسيط (Object أو جدول DB) بدل حسابات معقدة بالبداية، يسهل تطويره لاحقاً لحاسبة شحن ديناميكية.

---

## 5. الخطوات التالية المقترحة

1. تهيئة المشروع (`create-next-app` + Tailwind + Prisma) وربط قاعدة البيانات
2. بناء الـ schema والـ seed للمنتجات
3. بناء واجهة المتجر الأساسية (بدون دفع) — عرض المنتجات + السلة
4. دمج Stripe أولاً (أبسط تكامل وتوثيق)
5. دمج البوابة المحلية (Tap أو Moyasar) + الـ webhook
6. لوحة تحكم الأدمن لإدارة الطلبات والمنتجات
7. الإشعارات (إيميل/واتساب) وحاسبة الشحن
8. اختبار شامل ثم النشر (Vercel + قاعدة بيانات مُدارة)
